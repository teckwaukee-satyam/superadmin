<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - Admin Dashboard</title>
    
    <link rel="stylesheet" href="~/css/Final.css">
    
   
</head>
<body>
    <div class="min-h-screen flex">
        <!-- Sidebar -->
        <aside class="sidenav" id="sidenav-main">
            <div class="sidenav-header">
                <h4>Menu</h4>
                <i class="fas fa-times d-xl-none" id="iconSidenav"></i>
            </div>
            <hr class="horizontal">
            <div id="sidenav-collapse-main">
                <ul class="navbar-nav">
                    <li class="nav-item" id="li_dashboard">
                        <a href="/Index" class="nav-link active" id="anchDashboard">
                            <i class="ri-settings-line"></i>
                            <span class="nav-link-text">Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item" id="li_admin">
                        <a href="/Admin" class="nav-link" id="anchAdmin">
                            <i class="ri-user-3-line"></i>
                            <span class="nav-link-text">Add Admin</span>
                        </a>
                    </li>
                    <li class="nav-item" id="li_company">
                        <a href="/Company" class="nav-link" id="anchCompany">
                            <i class="ri-building-line"></i>
                            <span class="nav-link-text">Company</span>
                        </a>
                    </li>
                    <li class="nav-item" id="li_candidate">
                        <a href="/Candidate" class="nav-link" id="anchCandidate">
                            <i class="ri-user-line"></i>
                            <span class="nav-link-text">Candidates</span>
                        </a>
                    </li>
                    
                </ul>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col">
            <!-- Header -->
            @* <header class="border-b bg-white px-6 py-3 flex items-center justify-between"> *@
            @*     <h1 class="text-xl font-bold">Super Admin Panel</h1> *@
            @*     <div class="dropdown"> *@
            @*         <button class="dropdown-trigger relative h-8 w-8 rounded-full"> *@
            @*             <div class="avatar h-8 w-8 bg-primary text-primary-foreground flex items-center justify-center rounded-full"> *@
            @*                 A *@
            @*             </div> *@
            @*         </button> *@
            @*         <div class="dropdown-content w-56 hidden"> *@
            @*             <!-- Dropdown content --> *@
            @*         </div> *@
            @*     </div> *@
            @* </header> *@
           
            <!-- Page Content -->
            @RenderBody()
        </div>
    </div>

    @await RenderSectionAsync("Scripts", required: false)
    <script src="~/js/site.js"></script>
</body>
</html>